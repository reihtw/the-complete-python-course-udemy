{% extends 'base.jinja2' %}

{% block title %}
        <title>{{ post['title'] }}</title>
{% endblock %}

{% block content %}
        <h1>{{ post['title'] }}</h1>
        <p>{{ post['content'] }}</p>
{% endblock %}